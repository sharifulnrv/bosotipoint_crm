# ============================================================
# Southeast Landmark CRM — Requirements
# For cPanel / PythonAnywhere deployment (NO Docker needed)
# ============================================================

# Flask Core
Flask>=3.0.3
Flask-SQLAlchemy>=3.1.1
Flask-Migrate>=4.0.7
Flask-JWT-Extended>=4.6.0
Flask-Mail>=0.10.0
Flask-Limiter>=3.7.0
Flask-Cors>=4.0.1

# Database drivers (install the one you need)
PyMySQL>=1.1.1           # MySQL — cPanel / PythonAnywhere (most common)
# psycopg2-binary>=2.9.9  # PostgreSQL — only if your host offers it

# ORM & Migrations
SQLAlchemy>=2.0.31
alembic>=1.13.2

# In-process task scheduling (replaces Celery + Redis)
APScheduler>=3.10.4
filelock>=3.15.4

# Authentication & Security
pyotp>=2.9.0             # TOTP MFA
qrcode[pil]>=7.4.2       # QR code for MFA setup
bcrypt>=4.1.3
python-jose[cryptography]>=3.3.0
cryptography>=42.0.8

# Phone normalization
phonenumbers>=8.13.0

# Utilities
python-dotenv>=1.0.1
Werkzeug>=3.0.3
marshmallow>=3.21.3
flask-marshmallow>=1.2.1
requests>=2.32.3
Pillow>=10.4.0

# WSGI server (cPanel uses Passenger, PythonAnywhere uses their own)
# gunicorn is useful for testing locally
gunicorn>=22.0.0

# Mega Storage — upload call recordings to MEGA cloud
# (mega.py is vendored locally to avoid cPanel pip dependency conflicts)
tenacity>=8.0.0
pycryptodome>=3.9.6

# Backblaze B2 / AWS S3 — upload call recordings
boto3>=1.34.0
